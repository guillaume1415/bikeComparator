{% extends 'base.html.twig' %}

{% block title %}Marks{% endblock %}

{% block body %}
    <h1>Marks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mark.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mark.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marks_index') }}">back to list</a>

    <a href="{{ path('marks_edit', {'id': mark.id}) }}">edit</a>

    {{ include('marks/_delete_form.html.twig') }}
{% endblock %}
